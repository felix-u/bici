|00 :console_print

|0100

#00 :while dup #0a lt jni {
    &update_num_string jst
    &num_string *console_print write
    inc &while jmp
}
&newline *console_print write
break

:num_string {# "0 _20 }
:update_num_string dup #30 add &num_string #01 add store jmp;r
:newline {# _0a }
